1. SAMPLE DATASET (Initial Data)

from pyspark.sql import SparkSession

spark = SparkSession.builder.appName("Delta_Project").getOrCreate()

data = [
    (1, "C001", "Laptop", 50000),
    (2, "C002", "Mobile", 15000),
    (3, "C003", "Tablet", 20000),
    (4, "C004", "Laptop", 55000)
]

columns = ["id", "customer_id", "product", "amount"]

df = spark.createDataFrame(data, columns)
2. STUDENT TASKS (Give This First — Don’t Show Solution)
 TASK 1: Create Delta Table

 Store data as Delta format

 TASK 2: Insert New Data
(5, "C005", "Camera", 30000)
 TASK 3: Update Data

 Update:

id = 2 → amount = 18000
 TASK 4: Delete Data

 Delete:

id = 1
 TASK 5: MERGE (Incremental Load)

 New incoming data:

updates = [
    (3, "C003", "Tablet", 22000),  # update
    (6, "C006", "Watch", 8000)     # insert
]

 Perform UPSERT using MERGE

 TASK 6: Schema Evolution

 Add new column:

country
 TASK 7: Time Travel

 Show:

Previous version
Restore old data